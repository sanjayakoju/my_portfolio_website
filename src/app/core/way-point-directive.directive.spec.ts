import { WayPointDirectiveDirective } from './way-point-directive.directive';

describe('WayPointDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new WayPointDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
